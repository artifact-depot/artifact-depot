[package]
name = "depot-server"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"

# The shipped binary is still `depot` so users don't see the crate rename
# on the command line.
[[bin]]
name = "depot"
path = "src/main.rs"

[lib]
name = "depot_server"
path = "src/lib.rs"

[dependencies]
# Depot foundation
depot-core = { path = "../core", features = ["http", "openapi", "repo", "reqwest-errors"] }
depot-blob-file = { path = "../blob/file" }
depot-blob-s3 = { path = "../blob/s3" }
depot-format-apt = { path = "../format/apt" }
depot-format-cargo = { path = "../format/cargo" }
depot-format-docker = { path = "../format/docker" }
depot-format-golang = { path = "../format/golang" }
depot-format-helm = { path = "../format/helm" }
depot-format-npm = { path = "../format/npm" }
depot-format-pypi = { path = "../format/pypi" }
depot-format-yum = { path = "../format/yum" }
depot-kv-dynamo = { path = "../kv/dynamo", optional = true }
depot-kv-redb = { path = "../kv/redb" }
depot-ui = { path = "../ui" }

# Shared
anyhow = "1"
bytes = "1"
async-stream = "0.3"
async-trait = "0.1"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
sha2 = "0.10"
base64 = "0.23"
rand = "0.8"
reqwest = { version = "0.12", features = ["rustls-tls", "json", "multipart", "stream"], default-features = false }

# Server deps
axum = { version = "0.8", features = ["multipart"] }
http-body = "1"
http-body-util = "0.1"
blake3 = "1"
toml = "1.1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "registry"] }
opentelemetry = "0.32"
opentelemetry_sdk = { version = "0.32", features = ["rt-tokio", "logs"] }
opentelemetry-otlp = { version = "0.32", features = ["grpc-tonic", "trace", "logs"], default-features = false }
opentelemetry-appender-tracing = "0.32"
tracing-opentelemetry = "0.33"
utoipa = { version = "5", features = ["axum_extras"] }
utoipa-swagger-ui = { version = "9", features = ["axum"] }
uuid = { version = "1", features = ["v4", "serde"] }
governor = "0.10"
axum-server = { version = "0.8", features = ["tls-rustls"] }
rustls = { version = "0.23", features = ["aws_lc_rs"] }
tokio-util = { version = "0.7", features = ["io", "io-util"] }
tokio-stream = "0.1"
futures = "0.3"
mime_guess = "2"
tempfile = "3"
# v10 made the crypto backend a selectable provider; the default features no
# longer bundle one, so a provider feature must be enabled explicitly or every
# sign/verify call panics at runtime. rust_crypto is pure-Rust (hmac+sha2 for
# our HS256 tokens) and avoids a C/aws-lc-rs build dependency.
jsonwebtoken = { version = "11", features = ["rust_crypto"] }
argon2 = "0.6"
ldap3 = { version = "0.12", optional = true }
metrics = "0.24"
metrics-exporter-prometheus = "0.18"

# Used by server repo modules (apt, yum, helm)
tar = "0.4"
flate2 = "1"
lzma-rs = "0.3"
zip = { version = "2", default-features = false, features = ["deflate-flate2", "flate2"] }
chrono = { version = "0.4", features = ["serde"] }
rmp-serde = "1"
aes-gcm = "0.11"
arc-swap = "1"
moka = { version = "0.12", features = ["sync"] }
# Held at 1.x: bloomfilter 3.0 is a full API rewrite (sip_keys/bitmap/from_existing
# removed, set/check now fallible). The blob-GC reaper relies on those internals to
# build seed-identical shard filters and merge them; a mistranslation would treat
# live blobs as orphans. Migrate in a dedicated, test-covered PR, not a group bump.
bloomfilter = "3"
gethostname = "1.1"

[dev-dependencies]
depot-bench = { path = "../bench" }
depot-test-support = { path = "../test-support" }
# Enable test-support feature on depot-core for FailingKvStore / FailingBlobStore.
depot-core = { path = "../core", features = ["test-support"] }
rand_chacha = "0.9"
wiremock = "0.6"
http-body-util = "0.1"
tokio = { version = "1", features = ["test-util"] }
s3s = "0.13"
s3s-fs = "0.13"
hyper-util = { version = "0.1", features = ["tokio"] }

[features]
dynamodb = ["dep:depot-kv-dynamo"]
ldap = ["dep:ldap3"]
